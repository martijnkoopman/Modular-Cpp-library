language: cpp
matrix:
  include:
    - os: linux
	  dist: bionic
      compiler: gcc
      before_script:
        - mkdir build
        - cd build
        - cmake .. -DBUILD_SHARED_LIBS=ON -DBUILD_TESTING=ON -DBUILD_FOO_EXTENSION=ON
      script:
        - make
        - make test
